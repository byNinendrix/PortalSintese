{
  "name": "portal-sintese",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo da nova plataforma SINTESE",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @sintese/web dev",
    "dev:api": "pnpm --filter @sintese/api start:dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format"
  }
}

